# AIPIWEN 年龄分层解读方案
> 基于1441条真实用户数据 · 设计时间：2026-06-21

---

## 一、数据基础：我们的用户是谁

| 年龄段 | 人数 | 占比 | 特征 |
|--------|------|------|------|
| 0-3岁幼儿 | 18 | 1% | 几乎不存在，可暂不单独处理 |
| 4-6岁学前 | 92 | 6% | 家长测孩子，语言要极度具体 |
| **7-12岁学童** | **383** | **26%** | **核心用户群一，家长视角** |
| **13-18岁青少年** | **273** | **18%** | **核心用户群二，双视角** |
| 19-35岁青年 | 193 | 13% | 成人自测，职业/关系导向 |
| **36岁+中年** | **482** | **33%** | **最大单一群体，65%为女性（妈妈）** |

**核心结论：**
- 最大群体是 **36岁+的妈妈（33%）**，她们多数是来了解自己的，或者同时给孩子测了
- 第二大群体是 **7-12岁孩子（26%）**，报告实际读者是家长
- 两者加起来近60%，是产品的绝对主力用户

---

## 二、分层设计：4个年龄档位

### 分档规则

```
subjectAge 0-6   → tier: 'preschool'  （学前）
subjectAge 7-12  → tier: 'school'     （学童）  ← 占比最大，优先做精
subjectAge 13-18 → tier: 'teen'       （青少年）
subjectAge 19+   → tier: 'adult'      （成人）  ← 占比最大，优先做精
```

### 各档位核心差异

| 维度 | 学前 0-6 | 学童 7-12 | 青少年 13-18 | 成人 19+ |
|------|---------|-----------|------------|---------|
| **读者** | 父母 | 父母 | 父母+孩子本人 | 本人 |
| **人称** | 孩子/他/她 | 孩子/他/她 | 孩子/你（双轨） | 你 |
| **核心场景** | 养育信号识别 | 学习/班级/作业 | 升学/自我认知/情绪 | 职业/关系/自我理解 |
| **天赋引入方式** | "这个小孩天生就是…" | "这类孩子在学校会…" | "作为一个X型的人，你会…" | "你的天赋是…" |
| **行动建议方向** | 怎么陪伴 | 怎么辅导/沟通 | 怎么支持+孩子自己怎么用 | 自己怎么用 |

---

## 三、同一类型·不同年龄的语言差异示例

以**认知型**为例：

**学童（7-12岁）— 给家长看：**
> 认知型的孩子，不是不听话，是他必须先"想通"才能行动。你告诉他"因为我说了算"，他会叛逆；你告诉他"为什么这样做更好"，他会比任何孩子都配合。作业拖延的背后，常常是他还没找到做这件事的内在理由。

**青少年（13-18岁）— 给孩子本人和家长：**
> 你是认知型，这意味着你有一个别人没有的能力：别人接受的东西，你会追问"为什么"——这不是叛逆，是你的思维天赋在运转。高中最重要的事，是找到你愿意为之深度思考的那个方向，你的内驱力会比任何人都强。

**成人（19+岁）— 给本人：**
> 认知型的你，独立思考是最深的需求，也是最大的资产。你不擅长执行别人的逻辑，但一旦认同了某个方向，你的深度和持续性无人能及。职业选择的关键：要让自己"真正理解"这件事的意义，而不仅仅是被分配去做。

---

## 四、技术实现路径

### Step 1：API层加入年龄参数（改动最小）

**`api/guest-chat.js`** — 请求体新增 `subjectAge` 字段：

```js
// 现有
const { content, history, context, previousContext, sessionId } = payload;

// 改后
const { content, history, context, previousContext, sessionId, subjectAge } = payload;

// 年龄 → 档位映射
function getAgeTier(age) {
  if (!age || age < 0) return null;          // 无年龄数据，不分层
  if (age <= 6)  return 'preschool';
  if (age <= 12) return 'school';
  if (age <= 18) return 'teen';
  return 'adult';
}

const ageTier = getAgeTier(Number(subjectAge));
```

**system prompt 动态注入年龄上下文：**

```js
// 在 child context 的 prompt 中加入：
const AGE_CONTEXT = {
  preschool: `【被测者年龄：学前期（0-6岁）】
语言要求：极度具体，聚焦日常养育信号（吃饭/睡觉/情绪/探索），帮家长识别孩子天赋的早期表现。
避免：学业类建议、抽象概念。`,

  school: `【被测者年龄：学童期（7-12岁）】
语言要求：聚焦学校场景（作业、课堂、同伴关系），帮家长读懂孩子的学习行为背后的天赋逻辑。
避免：成人化表达、升学焦虑话题。`,

  teen: `【被测者年龄：青少年期（13-18岁）】
语言要求：双轨输出——既有给家长的"你的孩子是这样的"，也有直接对青少年说的"作为一个X型的你"。
聚焦：自我认知、情绪管理、升学方向选择、与父母关系。`,

  adult: `【被测者年龄：成人（18岁以上）】
语言要求：直接对话被测者本人，使用"你"而非"孩子"。
聚焦：职业匹配、人际关系、自我理解、天赋如何在工作中发挥。`,
};

// 注入到 child prompt（如果有年龄数据）
const ageContextNote = ageTier && AGE_CONTEXT[ageTier] ? `\n${AGE_CONTEXT[ageTier]}\n` : '';
```

### Step 2：前端传入年龄

**`fingerprint-v2-wizard.html`** — 生成报告时把年龄带上：

```js
// 已有年龄输入字段（表单Step里）
const subjectAge = document.getElementById('subject-age')?.value;

// 发送到 API 时加入
body: JSON.stringify({
  content,
  context: 'child',
  subjectAge: subjectAge || null,
  // ...其他字段
})
```

### Step 3：知识库补充年龄分层文案（工作量最大）

为每种 TRC 类型在知识库中增加 `age_tiers` 字段：

```js
// trc-knowledge-base.js 每个类型新增结构
age_tiers: {
  school:    { core: '...', behavior: '...', advice: '...' },
  teen:      { core: '...', behavior: '...', advice: '...' },
  adult:     { core: '...', behavior: '...', advice: '...' },
  preschool: { core: '...', behavior: '...', advice: '...' },
}
```

---

## 五、优先级与执行顺序

基于用户数据，按影响人数排优先级：

| 优先级 | 内容 | 覆盖用户 | 预期效果 |
|--------|------|---------|---------|
| **P0** | guest-chat.js 加 subjectAge 参数 + AGE_CONTEXT 注入 | 全部 1441 人 | 立即生效，AI自动调整语气 |
| **P1** | 学童（7-12）+ 成人（19+）知识库文案 | 865人（60%）| 最高价值用户体验提升 |
| **P2** | 青少年（13-18）知识库文案 | 273人（18%）| 解决青少年自测需求 |
| **P3** | 学前（0-6）知识库文案 | 110人（7%）| 完善，非紧急 |

---

## 六、执行计划

- [ ] **阶段一（本周）**：改 guest-chat.js，加 subjectAge + ageTier 动态 prompt — 0代码风险，立即上线
- [ ] **阶段二**：逐个为17种类型写 school + adult 两个档位的文案（先做占比最大的5个类型）
- [ ] **阶段三**：全量完成17×4个档位文案，更新 trc-knowledge-base.js
- [ ] **阶段四**：full-report.html 的类型卡片也按年龄段显示不同版本文案

---

*数据来源：fingerprint_labels.csv · 1441条有效记录 · 统计时间：2026-06-21*
