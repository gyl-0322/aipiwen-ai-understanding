# AIPIWEN 报告内容完整性 P0 Claude 复核交底

## 1. 复核定位

本轮是针对正式报告中“单指数值错乱、左右方向写反、固定模块被截断、强脑科学表达”的 P0 止损修复。

不改报告页面结构，不改 12 个固定模块的名称和顺序，不改用户四问的差异化呈现结构，不改姓名原始值。指导师为防重名在姓名后附加的数字或字母属于有效业务数据，必须原样保留。

## 2. 基线与隔离

- 基线：`origin/main@ef8f045`
- 隔离分支：`hotfix/report-content-integrity-p0`
- 隔离 worktree：`/private/tmp/aipiwen-report-content-integrity-p0`
- 主工作区存在其他未完成改动，本轮未读写、未回退该工作区。

## 3. 真实错误样本

测试十指数据：

| 位置 | R1 | L1 | R2 | L2 | R3 | L3 | R4 | L4 | R5 | L5 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| TRC | 14 | 17 | 12 | 13 | 7 | 16 | 15 | 14 | 11 | 12 |

由此得到：

- 精神 31，思维 25，体觉 23，听觉 29，视觉 23。
- 总 TRC 131，单指个人均值 13.1。
- 精神页必须写右拇 14、左拇 17，不得把合计 31 复制成两根拇指的数值。
- 体觉页必须识别左中 16 高于右中 7。
- 听觉 29 不是五大功能最高项，精神 31 才是当前样本最高合计。
- 总 TRC 131 不得与单指均值 13.1 直接写成高低比较。

## 4. 本轮修复

### 4.1 生成前数值契约

`api/generate-report.js` 在调用模型前校验：

- 10 根手指数值必须齐全，单指范围为 0-40。
- 五大功能合计必须与对应左右手指之和一致。
- 总 TRC 和个人均值必须能从 10 根手指原始数值重算得到。

### 4.2 Prompt 数据契约

- 两指合计只作总览，不与单指均值比较。
- 五个功能页分别注入左右手指的原始值、相对个人均值的位置、左右方向。
- 增加年龄和称谓一致性规则。
- 固定模块仍覆盖理解、个人化和应用，但禁止在前台显示“①是什么 / ②对你意味着什么 / ③怎么应用”这类模板标题。
- 用户问题按问题类型独立组织，不恢复统一三段式。

### 4.3 生成后完整性闸门

每个固定模块在返回前检查：

- 最低内容量和完整结束句，拦截模型半句截断。
- 五大功能页必须同时包含两根手指的正确原始值。
- 每根手指与个人均值的高低方向必须正确。
- 左右两根手指的高低方向必须正确。
- 非最高功能不得写成“最高/第一”。
- 拦截总 TRC 与单指均值的错误比较。
- 拦截“神经地图”“神经系统证明”“天生注定”等强断言。

任一检查不通过，该页不使用模型半成品，改用携带真实单指数值的安全自然表达补齐。

### 4.4 截断风险止损

- Preview 首轮实测表明“4 个固定模块一批 + 2800 tokens”会在当前 Qwen/Vercel 环境中整体触发 38 秒超时，因此不能采用。
- 调整为固定模块每 2 页一批、每批 1800 tokens；报告结构、模块名称和顺序不变。
- 用户问题每 2 题一批、每批 1300 tokens，降低四题共用一个长输出导致截断的风险。
- 第二轮 Preview 中 7 个批次仅 `core_2` 在 38 秒被中止，其余批次完成；因此单批等待调整为 50 秒，为 Vercel 60 秒上限保留约 10 秒收尾空间。
- 不增加模型重试；仍保留局部安全补齐，不将单批失败扩大为整份报告失败。

## 5. 已通过验证

- `node --check api/generate-report.js`
- `node --check scripts/test-report-content-integrity.js`
- `node scripts/test-report-content-integrity.js`
- `node scripts/test-report-issue-answer-modes.js`：12 类回答模式通过，4 个代表问题标题和正文差异保留。
- `node scripts/test-report-upload-p0.js`：12/12 通过。
- `node scripts/test-v3a-advisor-report-import.js`：93 项指导师报告导入检查通过；上传页和 V3 导入 API 均传入完整 `fingers`。
- `git diff --check`
- GitHub `report-issue-contract`：通过。
- Vercel Preview 真实模型调用：45 秒返回，`degraded:false`，12 个固定模块和 4 个问题全部返回。
- Preview 数值检查：右拇 14、左拇 17，未复制精神合计 31；左中 16 高于右中 7；听觉 29 未误写为最高。
- Preview 内容检查：无固定模块截断、无禁用强断言、4 个问题正文相互独立。

## 6. Claude 独立复核结论

- 代码层面通过，未发现新的 P0 阻塞问题。
- Claude 提出的中文省略号结尾缺口、0/40 边界测试和隐性脑科学断言缺口已补充并通过回归。
- 仓库调用方已核验：`report-upload.html` 与 `api/v3a-report-import.js` 均传入完整十指对象。
- Claude 对 2800 tokens 的速度估算与 Preview 实测不符；已依据三轮 Preview 收敛为“固定模块 2 页一批、问题 2 题一批、50 秒单批等待”。

## 7. 尚未证明的事项

- 尚未使用真实上传图片走完从识别到报告的全链路。
- 尚未部署生产环境。
- 尚未声称“线上已修好”。

下一道门应是：在当前 Preview 中由人工上传一份测试报告图片，走完识别、问题选择、生成和报告查看链路；确认页面内容与 API 结构化检查一致后，再决定是否进入生产发布审批。
