{
  "name": "@aipiwen/memory-engine-foundation",
  "version": "0.1.0-alpha.0",
  "private": true,
  "description": "AIPIWEN V3.1 Alpha Memory Engine Sprint 0 foundation contracts",
  "main": "src/index.js",
  "scripts": {
    "build": "node scripts/build-check.js",
    "check:node": "node --check src/contracts.js && node --check src/security.js && node --check src/v3-reader.js && node --check src/index.js && node --check scripts/build-check.js && node --check scripts/check-boundaries.js && node --check test/contracts.test.js && node --check test/security.test.js && node --check test/v3-reader.test.js",
    "test": "node --test test/*.test.js",
    "check:architecture": "node scripts/check-boundaries.js",
    "quality": "npm run build && npm run check:node && npm test && npm run check:architecture"
  }
}
